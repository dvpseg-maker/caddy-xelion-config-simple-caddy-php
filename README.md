Имя файла: README.md

Содержимое:

markdown
# Идеальный Caddy + PHP конфиг без лишней хуйни

> Работает из коробки. Никаких 502 ошибок. Для тех, кто ценит своё время.

## 🚀 Использование за 5 минут

1. **Скачайте** [Caddy](https://caddyserver.com/download) и [PHP-CGI](https://windows.php.net/download/)
2. **Создайте структуру папок**:
C:\Ваша-папка
├── caddy.exe
├── Caddyfile
├── html\ # файлы вашего сайта
└── php
└── php-cgi.exe

text
3. **Скопируйте конфиг ниже** в файл `Caddyfile`
4. **Запустите**:
```cmd
caddy run
php-cgi.exe -b 127.0.0.1:9000

🛡️ Конфигурация

Содержимое файла Caddyfile:

caddy
ваш-домен.ru, www.ваш-домен.ru {
    root * html
    file_server

    # Блокировка изменения файлов
    @writeBlock {
        path /*.xml /*.php /*.txt /*.json
        method PUT POST DELETE PATCH
    }
    respond @writeBlock 403

    # Блокировка просмотра системных папок
    @gsProtect {
        path /data/* /backups/* /plugins/* /themes/*
    }
    respond @gsProtect 404

    php_fastcgi 127.0.0.1:9000

    # Защита админки от интернета
    @admin {
        path /Admin/*
        not remote_ip 127.0.0.1
    }
    respond @admin 403
}

# Локальный доступ к админке
http://127.0.0.1:8080 {
    bind 127.0.0.1
    root * html
    file_server
    php_fastcgi 127.0.0.1:9000
}

# Adminer для работы с БД
http://localhost:8080 {
    bind 127.0.0.1
    handle /adminer.php {
        root php
        php_fastcgi 127.0.0.1:9000
    }
}
🎯 Особенности
🌐 Домен - автоматический HTTPS, доступ из интернета

🏠 127.0.0.1:8080 - полный доступ к сайту и админке локально

🗄️ localhost:8080 - только Adminer для баз данных

🛡️ Безопасность - админка недоступна из интернета

💡 Настройка под себя
Замените ваш-домен.ru на свой домен

Укажите правильный путь к админке (вместо /Admin/)

Настройте пути к системным папкам под вашу CMS

